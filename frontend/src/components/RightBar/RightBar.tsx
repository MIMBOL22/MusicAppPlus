import "./RightBar.css"
import {UserBar} from "../UserBar/UserBar.tsx";

export const RightBar = () => {
    return (
        <div className="right_bar">
            <UserBar/>
        </div>
    );
};